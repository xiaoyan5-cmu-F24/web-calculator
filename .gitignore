# Exclude compiled TypeScript files
/cmp

# Exclude build artifacts
/dist

# Exclude generated docs (GitHub Pages serves from /docs, not /dist)
/docs

# Exclude node_modules
/node_modules

# Exclude macOS system files
.DS_Store